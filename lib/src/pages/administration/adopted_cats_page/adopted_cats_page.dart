import 'package:flutter/material.dart';

class AdoptedCatsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Hello Adopted Cats"),
      ),
    );
  }
}